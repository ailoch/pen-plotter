{
    // settings related to the physical machine
    "machine": {
        "startPos": [128, 128, 9], // starting position of the nozzle
        "endPos": [128, 256], // final position of the nozzle
        "penOffset": [-38, 0], // position of the pen relative to the nozzle
        "plateSize": [256, 256], // size of the heatbed; lower-left corner is fixed at the origin
        "safeZoneSize": [217, 243], // size of the area the pen can safely move to without colliding
        "safeZoneOffset": [0, 13], // offset of the safe zone's lower-left corner from the origin, in pen space

        "canvasSize": [217, 243], // size of the paper/drawable surface - change this to change the paper size
        "canvasOffset": [0, 13] // offset of the canvas's (draw zone's) lower-left corner from the origin, in pen space
    },

    // controls how the pen moves while drawing
    "motion": {
        // line types: draw (controls all drawing line types, can be overridden), "perimeter", "infill", "gapInfill", "travel"
        "heights": {"draw": 2, "travel": 3.5, "_segmentBounds": 2.1, "_pathBounds": 2.2, "_documentBounds": 2.3}, // mm
        "speeds": {"draw": 30, "travel": 250}, // mm/s
        "accels": {"draw": 3000, "travel": 15000}, // mm/s^2
        "shortTravelThresholds": {"draw": 0.5, "infill": 1, "gapInfill": 1}, // travel moves below this distance (mm) will not lift the pen; a travel between two different roles uses the smaller of the two thresholds
        "loadDelay": 3 // wait this long for user to load pen
    },

    // controls what is drawn on paper
    "processing": {
        "tessellationTolerance": 0.012, // max allowed deviation (mm) of gcode from the true curve
        "infillSpacing": 0.3, // distance (mm) between concentric infill loops; 0 disables infill
        "generateGapInfill": true, // if true, adds extra strokes to fill small gaps in the infill

        "prefixFile": "gcode_templates/bambu_p1s_prefix.gcode",
        "suffixFile": "gcode_templates/bambu_p1s_suffix.gcode"
    },

    // controls how the gcode looks in bambu studio
    // these settings do not affect the printed result
    "visualization": {
        "penWidth": 0.35, // pen width in mm
        "showPenPos": true, // if false, nozzle position will be shown in slicer
        "objectHeightChange": true, // if true, slightly increases the height of every other object so they show as seperate layers when viewed in the slicer

        // leave empty to disable these
        "styleChangeMessage": "; FEATURE: %s", // printed before a line whose feature (draw role) changes; %s will be replaced by the feature name
        "layerChangeMessage": "; CHANGE_LAYER", // printed between objects when objectHeightChange is true

        "style": "role", // options are "role", "instruction", and "segment"
          // role: shows role of lines in slicer view (uses lineTypes)
          // instruction: shows g1/g2/g3 instructions with different colors (uses instructionTypes)
          // segment: shows each line segment as a different color, cycling through segmentTypes
        "lineTypes": {"perimeter": "Outer wall", "infill": "Internal solid infill", "gapInfill": "Gap infill", "_segmentBounds": "Support transition", "_pathBounds": "Support interface", "_documentBounds": "Support"},
        "instructionTypes": ["Sparse infill", "Support interface", "Overhang wall", "Gap infill"], // index 0 is G1, 1 is G2, 2 is G3, 3 is everything else
        "segmentTypes": ["Top surface", "Outer wall", "Inner wall", "Support", "Bridge", "Internal solid infill"]
    },

    // debug settings
    "debug": {
        "showBoundingBoxes": false,
        "optimizePathOrder": true, // if false, path order will be the same as in the input file
        "profiling": false // if true, profiles the pipeline and prints the slowest functions
    }
}
